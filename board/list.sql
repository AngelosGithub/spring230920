CREATE TABLE BOARD (
    Number INT,
    Name VARCHAR(8),
    Title VARCHAR(50),
    Content VARCHAR(4000),
    HitCount INT,
    Created DATETIME
);
SELECT * FROM BOARD;
INSERT INTO BOARD (Number, Name, Title, Content, HitCount, Created)
value ();